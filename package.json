{
  "name": "grievance-scout",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "bun test tests/all.test.ts --timeout 30000",
    "test:unit": "bun test tests/unit/ --timeout 30000",
    "test:integration": "bun test tests/integration/ --timeout 60000",
    "test:coverage": "bun test tests/all.test.ts --timeout 30000 --coverage",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'No linter configured'"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@libsql/client": "^0.17.0",
    "better-sqlite3": "^12.8.0",
    "commander": "^14.0.3",
    "date-fns": "^4.1.0"
  }
}
